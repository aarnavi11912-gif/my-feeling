<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>Will You Be Mine? 💜</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      min-height: 100vh;
      overflow: hidden;
      font-family: "Comic Sans MS", "Poppins", cursive, sans-serif;

      background:
        radial-gradient(circle at 20% 20%, #f7c8ff 0%, transparent 25%),
        radial-gradient(circle at 80% 10%, #dab6ff 0%, transparent 25%),
        linear-gradient(135deg, #4b176d, #7b2cbf, #c77dff);

      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }

    /* -------------------- BACKGROUND -------------------- */

    .background-hearts span {
      position: absolute;
      color: rgba(255, 255, 255, 0.35);
      font-size: 25px;
      animation: floatHeart 8s linear infinite;
    }

    .background-hearts span:nth-child(1) {
      left: 5%;
      animation-delay: 0s;
    }

    .background-hearts span:nth-child(2) {
      left: 15%;
      animation-delay: 2s;
      font-size: 38px;
    }

    .background-hearts span:nth-child(3) {
      left: 30%;
      animation-delay: 5s;
      font-size: 20px;
    }

    .background-hearts span:nth-child(4) {
      left: 55%;
      animation-delay: 1s;
      font-size: 35px;
    }

    .background-hearts span:nth-child(5) {
      left: 72%;
      animation-delay: 4s;
    }

    .background-hearts span:nth-child(6) {
      left: 88%;
      animation-delay: 6s;
      font-size: 42px;
    }

    @keyframes floatHeart {
      0% {
        bottom: -80px;
        transform: translateX(0) rotate(0deg);
        opacity: 0;
      }

      20% {
        opacity: 1;
      }

      100% {
        bottom: 110%;
        transform: translateX(70px) rotate(360deg);
        opacity: 0;
      }
    }

    /* -------------------- BALLOONS -------------------- */

    .balloon {
      position: absolute;
      width: 70px;
      height: 90px;
      border-radius: 50%;
      box-shadow: inset -10px -10px 20px rgba(0, 0, 0, 0.15);
      animation: balloonMove 4s ease-in-out infinite alternate;
      z-index: 1;
    }

    .balloon::before {
      content: "";
      position: absolute;
      width: 2px;
      height: 130px;
      background: rgba(255, 255, 255, 0.7);
      left: 50%;
      top: 88px;
    }

    .balloon::after {
      content: "";
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
      bottom: -5px;
      width: 12px;
      height: 12px;
      background: inherit;
      clip-path: polygon(50% 0, 100% 100%, 0 100%);
    }

    .balloon1 {
      background: #ff8fab;
      left: 5%;
      top: 10%;
    }

    .balloon2 {
      background: #e0aaff;
      left: 13%;
      top: 20%;
      animation-delay: 1s;
    }

    .balloon3 {
      background: #ffb3c6;
      right: 6%;
      top: 12%;
      animation-delay: 2s;
    }

    .balloon4 {
      background: #c8b6ff;
      right: 15%;
      top: 26%;
      animation-delay: 0.5s;
    }

    @keyframes balloonMove {
      from {
        transform: translateY(0) rotate(-3deg);
      }

      to {
        transform: translateY(-25px) rotate(3deg);
      }
    }

    /* -------------------- MAIN CARD -------------------- */

    .proposal-card {
      width: min(950px, 92%);
      min-height: 590px;

      background: rgba(255, 255, 255, 0.18);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);

      border: 2px solid rgba(255, 255, 255, 0.35);
      border-radius: 35px;

      box-shadow:
        0 25px 60px rgba(45, 0, 70, 0.4),
        inset 0 1px 1px rgba(255, 255, 255, 0.4);

      position: relative;
      z-index: 10;

      display: flex;
      align-items: center;
      justify-content: space-around;
      gap: 30px;

      padding: 40px;
    }

    /* -------------------- GIRL -------------------- */

    .girl-section {
      position: relative;
      width: 330px;
      height: 450px;

      display: flex;
      align-items: center;
      justify-content: center;
    }

    .girl {
      position: relative;
      width: 260px;
      height: 400px;
    }

    .hair-back {
      position: absolute;
      width: 175px;
      height: 200px;

      background: #3b213f;
      border-radius: 90px 90px 50px 50px;

      left: 42px;
      top: 15px;
    }

    .head {
      position: absolute;
      width: 130px;
      height: 145px;

      background: #ffd6c9;
      border-radius: 50% 50% 46% 46%;

      left: 65px;
      top: 40px;

      z-index: 2;
    }

    .hair-front {
      position: absolute;
      width: 130px;
      height: 75px;

      background: #3b213f;

      border-radius: 70px 70px 20px 40px;

      left: 65px;
      top: 35px;

      z-index: 4;

      clip-path: polygon(
        0 0,
        100% 0,
        100% 50%,
        75% 32%,
        65% 65%,
        45% 30%,
        25% 60%,
        0 45%
      );
    }

    .eye {
      position: absolute;
      top: 80px;

      width: 12px;
      height: 14px;

      background: #40283f;
      border-radius: 50%;

      z-index: 5;
    }

    .eye.left {
      left: 100px;
    }

    .eye.right {
      left: 150px;
    }

    .blush {
      position: absolute;
      top: 108px;

      width: 25px;
      height: 12px;

      background: rgba(255, 108, 148, 0.35);
      border-radius: 50%;

      z-index: 5;
    }

    .blush.left {
      left: 82px;
    }

    .blush.right {
      left: 154px;
    }

    .smile {
      position: absolute;

      width: 30px;
      height: 15px;

      border-bottom: 4px solid #b85070;
      border-radius: 0 0 30px 30px;

      left: 115px;
      top: 118px;

      z-index: 5;
    }

    .body {
      position: absolute;

      width: 170px;
      height: 210px;

      background: linear-gradient(#efb6ff, #aa62d9);

      border-radius: 70px 70px 30px 30px;

      left: 45px;
      top: 165px;

      z-index: 1;
    }

    .dress-bottom {
      position: absolute;

      width: 220px;
      height: 120px;

      background: #b86ae5;

      left: 20px;
      top: 300px;

      border-radius: 50% 50% 25px 25px;

      clip-path: polygon(25% 0, 75% 0, 100% 100%, 0 100%);
    }

    .arm {
      position: absolute;

      width: 38px;
      height: 150px;

      background: #ffd6c9;

      border-radius: 30px;

      z-index: 3;
    }

    .arm.left {
      left: 48px;
      top: 190px;
      transform: rotate(-30deg);
    }

    .arm.right {
      right: 48px;
      top: 190px;
      transform: rotate(30deg);
    }

    /* -------------------- BOUQUET -------------------- */

    .bouquet {
      position: absolute;

      left: 76px;
      top: 235px;

      width: 115px;
      height: 130px;

      z-index: 10;
    }

    .flower {
      position: absolute;

      width: 40px;
      height: 40px;

      background: #ff4d8d;

      border-radius: 50%;

      box-shadow:
        12px 0 #ff75a7,
        -12px 0 #ff75a7,
        0 12px #ff75a7,
        0 -12px #ff75a7;
    }

    .flower::after {
      content: "";

      position: absolute;

      width: 15px;
      height: 15px;

      background: #ffdf5d;

      border-radius: 50%;

      top: 12px;
      left: 12px;
    }

    .flower1 {
      top: 5px;
      left: 37px;
    }

    .flower2 {
      top: 35px;
      left: 10px;
    }

    .flower3 {
      top: 40px;
      right: 5px;
    }

    .wrap {
      position: absolute;

      width: 95px;
      height: 90px;

      background: #f9c5d5;

      top: 45px;
      left: 12px;

      clip-path: polygon(0 0, 100% 0, 67% 100%, 35% 100%);

      z-index: -1;
    }

    .ribbon {
      position: absolute;

      top: 80px;
      left: 38px;

      font-size: 40px;
    }

    /* -------------------- MESSAGE -------------------- */

    .message-section {
      flex: 1;
      max-width: 480px;
      text-align: center;
      color: white;
    }

    .cute-title {
      font-size: 18px;
      color: #f8dcff;
      margin-bottom: 12px;
    }

    .message-section h1 {
      font-size: clamp(38px, 6vw, 67px);
      line-height: 1.05;
      text-shadow: 0 6px 20px rgba(74, 0, 100, 0.4);
      margin-bottom: 15px;
    }

    .message-section h1 span {
      color: #ffd6ff;
    }

    .message-section p {
      font-size: 19px;
      line-height: 1.6;
      color: #f8eaff;
      margin-bottom: 15px;
    }

    .question {
      font-size: 29px !important;
      font-weight: bold;
      color: white !important;
      margin: 25px 0 !important;
    }

    /* -------------------- BUTTONS -------------------- */

    .button-area {
      position: relative;
      width: 100%;
      height: 110px;
      margin-top: 10px;
    }

    button {
      border: none;
      cursor: pointer;

      padding: 15px 35px;

      border-radius: 50px;

      font-size: 20px;
      font-weight: bold;

      transition: 0.25s;

      position: absolute;

      box-shadow: 0 8px 18px rgba(42, 5, 60, 0.25);
    }

    #yesBtn {
      background: linear-gradient(135deg, #ff77b7, #ff4b91);
      color: white;

      left: 20%;
      top: 20px;
    }

    #yesBtn:hover {
      transform: scale(1.15);
      box-shadow:
        0 10px 25px rgba(255, 62, 143, 0.5);
    }

    #noBtn {
      background: white;
      color: #7b2cbf;

      right: 20%;
      top: 20px;
    }

    /* -------------------- STICKERS -------------------- */

    .sticker {
      position: absolute;
      font-size: 45px;
      animation: stickerFloat 3s ease-in-out infinite alternate;
      z-index: 20;
    }

    .sticker1 {
      left: 15px;
      top: 15px;
    }

    .sticker2 {
      right: 20px;
      bottom: 20px;
      animation-delay: 1s;
    }

    .sticker3 {
      right: 25px;
      top: 20px;
      animation-delay: 0.5s;
    }

    .sticker4 {
      left: 30px;
      bottom: 15px;
      animation-delay: 1.5s;
    }

    @keyframes stickerFloat {
      from {
        transform: translateY(0) rotate(-7deg);
      }

      to {
        transform: translateY(-10px) rotate(7deg);
      }
    }

    /* -------------------- SUCCESS MODAL -------------------- */

    .success-screen {
      position: fixed;
      inset: 0;

      background: rgba(73, 8, 107, 0.88);
      backdrop-filter: blur(14px);

      z-index: 1000;

      display: none;
      align-items: center;
      justify-content: center;

      padding: 20px;
    }

    .success-card {
      width: min(600px, 95%);

      background: white;

      border-radius: 35px;

      text-align: center;

      padding: 50px 35px;

      color: #7923a6;

      box-shadow:
        0 25px 70px rgba(0, 0, 0, 0.35);

      animation: successPop 0.6s ease;
    }

    @keyframes successPop {
      from {
        transform: scale(0.5);
        opacity: 0;
      }

      to {
        transform: scale(1);
        opacity: 1;
      }
    }

    .success-card .big-heart {
      font-size: 100px;
      animation: heartbeat 0.8s infinite alternate;
    }

    @keyframes heartbeat {
      from {
        transform: scale(1);
      }

      to {
        transform: scale(1.17);
      }
    }

    .success-card h2 {
      font-size: 45px;
      margin: 10px 0;
    }

    .success-card p {
      font-size: 20px;
      line-height: 1.6;
    }

    /* -------------------- CONFETTI -------------------- */

    .confetti {
      position: fixed;

      width: 12px;
      height: 12px;

      top: -20px;

      border-radius: 3px;

      animation: confettiFall linear forwards;

      z-index: 2000;
    }

    @keyframes confettiFall {
      to {
        transform: translateY(110vh) rotate(900deg);
        opacity: 0;
      }
    }

    /* -------------------- RESPONSIVE -------------------- */

    @media (max-width: 800px) {
      body {
        overflow-y: auto;
        padding: 30px 0;
      }

      .proposal-card {
        flex-direction: column;
        min-height: auto;
        padding: 20px;
      }

      .girl-section {
        transform: scale(0.78);
        height: 340px;
      }

      .message-section {
        margin-top: -30px;
      }

      .message-section h1 {
        font-size: 45px;
      }

      .message-section p {
        font-size: 17px;
      }

      #yesBtn {
        left: 8%;
      }

      #noBtn {
        right: 8%;
      }

      .balloon {
        transform: scale(0.7);
      }
    }
  </style>
</head>

<body>

  <!-- Floating Background Hearts -->
  <div class="background-hearts">
    <span>💜</span>
    <span>💗</span>
    <span>💕</span>
    <span>💜</span>
    <span>💖</span>
    <span>💗</span>
  </div>

  <!-- Balloons -->
  <div class="balloon balloon1"></div>
  <div class="balloon balloon2"></div>
  <div class="balloon balloon3"></div>
  <div class="balloon balloon4"></div>

  <!-- Main Proposal Card -->
  <main class="proposal-card">

    <!-- Stickers -->
    <div class="sticker sticker1">🧸</div>
    <div class="sticker sticker2">💌</div>
    <div class="sticker sticker3">✨</div>
    <div class="sticker sticker4">🌸</div>

    <!-- Girl Character -->
    <section class="girl-section">

      <div class="girl">

        <div class="hair-back"></div>

        <div class="head"></div>

        <div class="hair-front"></div>

        <div class="eye left"></div>
        <div class="eye right"></div>

        <div class="blush left"></div>
        <div class="blush right"></div>

        <div class="smile"></div>

        <div class="body"></div>

        <div class="dress-bottom"></div>

        <div class="arm left"></div>
        <div class="arm right"></div>

        <!-- Bouquet -->
        <div class="bouquet">

          <div class="flower flower1"></div>
          <div class="flower flower2"></div>
          <div class="flower flower3"></div>

          <div class="wrap"></div>

          <div class="ribbon">🎀</div>

        </div>

      </div>

    </section>

    <!-- Proposal Text -->
    <section class="message-section">

      <div class="cute-title">
        ✨ A little question from my heart ✨
      </div>

      <h1>
        I <span>Love</span> You 💜
      </h1>

      <p>
        You make my ordinary days feel special,
        my smiles brighter, and my heart a little happier.
      </p>

      <p>
        So today, with these flowers and all my courage,
        I just want to ask you something...
      </p>

      <p class="question">
        Will you be mine? 🥺💜
      </p>

      <div class="button-area" id="buttonArea">

        <button id="yesBtn">
          Yes 💜
        </button>

        <button id="noBtn">
          No 🙈
        </button>

      </div>

    </section>

  </main>

  <!-- Success Screen -->
  <div class="success-screen" id="successScreen">

    <div class="success-card">

      <div class="big-heart">
        💜
      </div>

      <h2>
        Yaaayyy! 🥰
      </h2>

      <p>
        You just made me the happiest girl in the world! 💕
      </p>

      <p>
        Our beautiful little love story starts here. 🌸✨
      </p>

      <p style="font-size: 35px; margin-top: 20px;">
        👩‍❤️‍👨
      </p>

    </div>

  </div>

  <script>

    const yesBtn = document.getElementById("yesBtn");
    const noBtn = document.getElementById("noBtn");

    const buttonArea = document.getElementById("buttonArea");
    const successScreen = document.getElementById("successScreen");


    /* -------------------- YES BUTTON -------------------- */

    yesBtn.addEventListener("click", () => {

      successScreen.style.display = "flex";

      createConfetti();

      createCelebrationHearts();

    });


    /* -------------------- MOVING NO BUTTON -------------------- */

    function moveNoButton() {

      const areaRect = buttonArea.getBoundingClientRect();

      const btnWidth = noBtn.offsetWidth;
      const btnHeight = noBtn.offsetHeight;

      const maxX = areaRect.width - btnWidth;
      const maxY = areaRect.height - btnHeight;

      const randomX = Math.random() * maxX;
      const randomY = Math.random() * maxY;

      noBtn.style.left = randomX + "px";
      noBtn.style.top = randomY + "px";

      noBtn.style.right = "auto";
    }


    noBtn.addEventListener("mouseenter", moveNoButton);

    noBtn.addEventListener("touchstart", (event) => {

      event.preventDefault();

      moveNoButton();

    });


    /* -------------------- CONFETTI -------------------- */

    function createConfetti() {

      const colors = [
        "#7b2cbf",
        "#c77dff",
        "#ff4d8d",
        "#ff85b3",
        "#ffd6ff",
        "#ffffff",
        "#ffdf5d"
      ];

      for (let i = 0; i < 140; i++) {

        const confetti = document.createElement("div");

        confetti.classList.add("confetti");

        confetti.style.left =
          Math.random() * 100 + "vw";

        confetti.style.background =
          colors[Math.floor(Math.random() * colors.length)];

        confetti.style.width =
          Math.random() * 10 + 5 + "px";

        confetti.style.height =
          Math.random() * 10 + 5 + "px";

        confetti.style.animationDuration =
          Math.random() * 3 + 2 + "s";

        confetti.style.animationDelay =
          Math.random() * 1 + "s";

        document.body.appendChild(confetti);

        setTimeout(() => {
          confetti.remove();
        }, 6000);

      }

    }


    /* -------------------- CELEBRATION HEARTS -------------------- */

    function createCelebrationHearts() {

      const emojis = [
        "💜",
        "💖",
        "💕",
        "💗",
        "🌸",
        "✨"
      ];

      for (let i = 0; i < 45; i++) {

        const heart = document.createElement("div");

        heart.innerText =
          emojis[Math.floor(Math.random() * emojis.length)];

        heart.style.position = "fixed";

        heart.style.left =
          Math.random() * 100 + "vw";

        heart.style.bottom = "-50px";

        heart.style.fontSize =
          Math.random() * 30 + 20 + "px";

        heart.style.zIndex = "1500";

        heart.style.pointerEvents = "none";

        heart.style.transition =
          `all ${Math.random() * 3 + 3}s linear`;

        document.body.appendChild(heart);

        setTimeout(() => {

          heart.style.bottom = "110vh";

          heart.style.transform =
            `translateX(${Math.random() * 200 - 100}px)
             rotate(${Math.random() * 720}deg)`;

          heart.style.opacity = "0";

        }, 50);

        setTimeout(() => {

          heart.remove();

        }, 6500);

      }

    }

  </script>

</body>
</html>
